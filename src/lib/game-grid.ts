// Shared column-density for every game-tile grid on the site (home, discover,
// library, favorites, recent, continue-playing). Keeping this in one place
// means "show more games per row" is a single edit, not six inconsistent ones.
export const GAME_GRID_COLS = "grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-4 xl:grid-cols-5 2xl:grid-cols-6 min-[1700px]:grid-cols-7";

